<ul class="tabs">
	{% for route, name in tabs %}
		<li class="{{ route == current ? 'active' : '' }}">
			<a href="{{ url(route, {userID: userID}) }}" data-liv>{{ name }}</a>
		</li>
	{% endfor %}
</ul>
